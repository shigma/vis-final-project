/**
 * Some Used Events:
 * 1. keyword-changed
 *    parameter: a string representing the new keyword.
 * 2. user-changed
 *    parameter: the user ID (a number)
 * 3. date-filter-changed
 *    parameter: an array like [beginDate, endDate]. beginDate and endDate 
 *    must be Javascript Date() Object.
 */
module.exports = new Vue();